# Read the size of the array
n = int(input())

# Read the array elements
arr = list(map(int, input().split()))

# Initialize sum
s = 0

# Traverse all odd index positions (1, 3, 5, ...)
for i in range(1, n, 2):
    s += arr[i]

# Print the sum of elements at odd indices
print(s)
