# Read the size of the array
n = int(input())

# Read the array elements
l = list(map(int, input().split()))

# Find the integer average of the array
avg = sum(l) // n

# Check if the average is present in the array
if avg in l:
    print("True")
else:
    print("False")
