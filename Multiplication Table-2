# Take two inputs: A (number), B (number of terms)
A, B = map(int, input().split())

# Loop from 1 to B (inclusive)
for i in range(1, B + 1):
    
    # Print multiplication in required format
    print(f"{A} x {i} = {A*i}")
