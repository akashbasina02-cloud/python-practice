t = int(input())

for _ in range(t):
    n = int(input())
    s = input()

    count = 0

    for ch in s:
        if ch in "aeiou":
            count = 0
        else:
            count += 1

        if count >= 4:
            print("NO")
            break
    else:
        print("YES")
